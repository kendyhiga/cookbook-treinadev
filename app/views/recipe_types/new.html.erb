<h1>Cadastrar novo tipo de receita</h1>

<% if @recipe_type.errors.any? %>
  <%= flash[:failure] %>
<% end %>

<%= form_for @recipe_type do |f| %>

  <%= f.label :name, 'Nome' %>
  <%= f.text_field :name %>

  <%= f.submit 'Cadastrar' %>
<% end %>

<%= link_to 'Voltar', root_path %>
