<h1><%= (t 'register_new_list') %></h1>

<% if @list.errors.any? %>
  <%= flash[:failure] %>
<% end %>

<%= form_for @list do |f| %>

  <%= f.label :name, (t 'name') %>
  <%= f.text_field :name %>

  <%= f.submit (t 'register') %>
<% end %>

<%= link_to (t 'back'), root_path %>
